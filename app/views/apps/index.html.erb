<div class="row">
	<div class="col-md-3">
		tuy chon tim kiem
	</div>
	<div class="col-md-9">
		<div class="row">
			<% @apps.each do |app| %>
				<div class="col-md-4">
					<%= image_tag app.picture%> <br>
					<%= app.name||"アプリケーションの名" %> <br>
					<%= app.rating||"Rating:        *****" %>
				</div>
			<% end %>
		</div>		
	</div>
</div>