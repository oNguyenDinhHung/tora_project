<nav class="navbar navbar-expand-md navbar-light" style="background-color: #dbd80a;margin-bottom: 50px;">
  <%=link_to t('websitename'),root_path, class: 'active navbar-brand ml-5' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav ml-auto mr-4">
    </ul>

<!-- <ul class="nav navbar-nav">
<li><%=link_to t("views.app.new_app"),new_app_path if user_signed_in? %></li>
<li><%=link_to t("views.maker.maker_list"),makers_path if user_signed_in?&&current_user.admin? %></li>
<li><%=link_to t("views.store.store_list"),stores_path if user_signed_in?&&current_user.admin? %></li>
</ul> -->
<%=link_to t("views.app.new_app"),new_app_path,class: "mr-2 active btn btn-warning" if user_signed_in? %>

<div class="dropdown my-dropdown mr-5">
  <a class="mr-1" data-toggle="dropdown">
    <% if user_signed_in? %>
    <%= current_user.email %>
    <% else %>
    新しいユーザー ?
    <% end %>
  </a>
  <ul class="dropdown-menu">

    <li class="nav-item active mr-2"><%=link_to t('profile'),edit_user_registration_path if user_signed_in? %></li>
    <li class="nav-item active mr-2"><%=link_to t("views.maker.maker_list"),makers_path if user_signed_in?&&current_user.admin? %></li>
    <li class="nav-item active mr-2"><%=link_to t("views.store.store_list"),stores_path if user_signed_in?&&current_user.admin? %></li>
    <li class="nav-item active mr-2"><%=link_to t("sign_up"),new_user_registration_path,class: "glyphicon glyphicon-user" if !user_signed_in? %></li>
    <li class="nav-item active mr-2"><%=link_to t("log_in"),new_user_session_path,class: "glyphicon glyphicon-log-in" if !user_signed_in? %></li>
    <li class="nav-item active mr-2"><%=link_to t("sign_out"),destroy_user_session_path,method: :delete,class: "glyphicon glyphicon-log-out" if user_signed_in? %></li>

  </ul>
</div>

<div class="dropdown mr-5">
  <a class="mr-5" data-toggle="dropdown">
    <button class="btn my-2 my-sm-0" type="submit"><i class="fa fa-cog" aria-hidden="true"></i></button>
  </a>
  <ul class="dropdown-menu">
    <li><a href="#">マイ アプリ</a>
    </li>
    <li><a href="#">言語</a>
    </li>
    <li><a href="#">プライバシー ポリシー</a>
    </li>
    <li><a href="#">利用規約</a>
    </li>
    <li><a href="#">プライバシー ポリシー</a>
    </li>
  </ul>
</div>

</div>
</nav>